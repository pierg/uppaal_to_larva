Imports
{
	import SocketServerPackage.EchoServer;
}

Global{

	Events{
		rlevent(obs, preobs) = {EchoServer.rlevent()}
		reset() = {EchoServer.reset()}
	}

	Property reward{

		States{
			Normal{<<<UPPAAL_STATES>>>}
			Starting{start}
		}

		Transitions{
			<<<UPPAAL_TRANSITIONS>>>
		}

	}

}
